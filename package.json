{
  "name": "@stackeat/primitives",
  "version": "0.1.3",
  "main": "lib/exports.js",
  "repository": "https://github.com/stackeat/elementary.git",
  "author": "eduard@stackeat.com",
  "license": "MIT",
  "directories": {
    "lib": "lib"
  },
  "files": [
    "lib"
  ],
  "publishConfig": {
    "access": "restricted"
  },
  "scripts": {
    "build": "ttsc --build --force",
    "watch": "ttsc --build --watch",
    "rebuild": "yarn clean && yarn build",
    "postversion": "git push --tags && git push",
    "prepack": "yarn rebuild",
    "clean": "rm -rf lib"
  },
  "devDependencies": {
    "@zerollup/ts-transform-paths": "^1.7.3",
    "ttypescript": "^1.5.7",
    "typescript": "^3.5.3"
  }
}
